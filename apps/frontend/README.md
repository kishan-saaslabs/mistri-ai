# @mistri-ai/frontend

Vite + React Router UI for Mistri AI. Tailwind CSS and shadcn/ui provide theming and primitives. Design follows the product mock (Calls, Deals, Ask Mistri).

From the repo root:

```bash
pnpm --filter @mistri-ai/frontend dev
```
